#pragma once

const float framerate = 100; // the framerate
const bool jerky = false;  // movment type (don't recommend setting to true)
const int tex_size = 32;  // the "quality" of images. Every texture's sides are divisible by tex_size